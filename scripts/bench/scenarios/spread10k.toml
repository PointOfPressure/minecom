# Scenario (b), MASTERPLAN §4 P0: N bots spread over a pregenerated
# 10,000-chunk (100x100) world. Blocked as of 2026-07-15 by the rust-mc-bot
# connection bug — see docs/HANDOFF.md's escalation entry.
name = "spread10k"
mode = "live"
description = "N bots spread across a pregenerated 100x100-chunk (10,000-chunk) world"
duration_seconds = 180
bots = 100
bot_join_batch = 100
world = "pregen"
pregen_radius_chunks = 50
pregen_seed = 20260708
bench_scenario_env = "spread"
bench_extra_env = { MINECOM_BENCH_SPREAD_RADIUS = "50" }
join_hold_seconds = 10
join_timeout_seconds = 60
ramp = false
